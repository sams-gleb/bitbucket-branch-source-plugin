<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry title="${%Communicate Unstable builds to Bitbucket as Successful}" field="sendSuccessNotificationForUnstableBuild">
    <f:checkbox/>
  </f:entry>
  <f:entry title="${%Send notifications for PR builds only}" field="sendNotificationsForPrBuildsOnly">
    <f:checkbox/>
  </f:entry>
</j:jelly>